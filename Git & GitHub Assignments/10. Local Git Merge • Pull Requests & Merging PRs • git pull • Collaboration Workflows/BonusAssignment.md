### Bonus Assignment – Mini Collaboration Simulation (Optional)

**Goal:** Simulate a small team workflow (can be done alone with two clones or with a classmate).

1. Person A (or Clone A): Create branch `feature-A`, add a file, push, open PR.
2. Person B (or Clone B): Create branch `feature-B`, add a different file, push, open another PR.
3. Merge both PRs on GitHub (one after another).
4. Both persons run `git checkout main && git pull` and confirm both files are present.
5. Write 2–3 lines about what you observed when pulling after multiple merges.

**Answers** 

Pulling after multiple merge shows a little difference as it shows 2/2 files done.
also it shows long pull message after successful pull unlike pull of a single merge.
<img width="650" height="374" alt="feaeture-a" src="https://github.com/user-attachments/assets/82393b16-a7f9-4eca-b7d2-7a4d25214e7c" />
<img width="678" height="413" alt="feature-b" src="https://github.com/user-attachments/assets/9a98c033-28f0-49d0-8bdf-b084d08f4f11" />
<img width="601" height="230" alt="git log --oneline" src="https://github.com/user-attachments/assets/5a04ad5a-6674-41a2-a222-958e5cc6a2d3" />

---
